/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.finlibre.app',
  appName: 'FinLibre',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
